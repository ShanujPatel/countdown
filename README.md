# Countdown App

Simple React + Vite app that counts down to 27/08/2026.

Quick start:

```bash
npm install
npm run dev
```

Open the URL shown by Vite (usually http://localhost:5173).

Target date: 27/08/2026 (local timezone, midnight).
